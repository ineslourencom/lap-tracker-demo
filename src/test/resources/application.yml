spring:
  application:
  name: lap-tracker-service
  datasource:
    # url is overridden by BaseITTest
    hikari:
      maximum-pool-size: 3
      
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        generate_statistics: true

  flyway:
    enabled: false
